(last updated 2017-12-22)

Designated expert note:

CoAP registers different URI schemes for accessing CoAP resources 
via different protocols. This approach runs counter to the WWW 
principle that a URI identifies a resource and that multiple URIs for 
identifying the same resource should be avoided [R1].

[R1] http://www.w3.org/TR/webarch/#avoid-uri-aliases