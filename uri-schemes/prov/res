(last updated 2012-09-12)

Resource Identifier (RI) Scheme name: res
Status: provisional

Scheme syntax:
   res://ieframe.dll/<File> (Where <File> is the file to load)

Scheme semantics:
   Used by Internet Explorer to display error pages when the 
   server doesn't have its own customized error pages, or when 
   there is no response from the server (in case which the 
   server wasn't found, like when the server is down or the 
   domain isn't registered or when there is no Internet 
   connection, or in case of a timeout).

Encoding considerations:
   Unknown, use with care.

Applications/protocols that use this scheme name:
   Works only in Internet Explorer.

Interoperability considerations:
   Unknown, use with care.
Security considerations:
   Unknown, use with care.
Contact:
   Registering party: Alexey Melnikov <alexey.melnikov&isode.com>
   Scheme creator: Microsoft
Author/Change controller:
   Either the registering party or someone who is verified to represent
   the scheme creator.  See previous answer.
References:
  http://msdn.microsoft.com/en-us/library/aa767740(v=vs.85).aspx 

(file created 2012-09-12)