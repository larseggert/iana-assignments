(last updated 2012-09-23)

Resource Identifier (RI) Scheme name: chrome 
Status: provisional

Scheme syntax:
   chrome://<package>/<section>/<path> (Where <section> is either "content", "skin" or "locale")

Scheme semantics:
   Specifies user interfaces built using XUL in Mozilla-based browsers.

Encoding considerations:
   Unknown, use with care.

Applications/protocols that use this scheme name:
   Works only in Mozilla-based browsers such as Firefox, SeaMonkey and Netscape. Not related to the Google Chrome browser.

Interoperability considerations:
   Unknown, use with care. 
   May be unsuitable for open use on the public internet.
Security considerations:
   Unknown, use with care.
Contact:
   Registering party: Dave Thaler <dthaler&microsoft.com>
   Scheme creator: Mozilla
Author/Change controller:
   Either the registering party or someone who is verified to represent
   the scheme creator.  See previous answer.
References:
   https://developer.mozilla.org/en/XUL_Tutorial/The_Chrome_URL  

Resource Identifier (RI) Scheme name: chrome 
Status: provisional

Scheme syntax:
   chrome://<settings>/<path>/[<specificSetting>] (Where <settings> is either "chrome" or "feedback"; When <settings> is "chrome", <path> is either "history", "extensions", "settings" or "help"; Where <specificSetting> is either "syncSetup" or "clearBrowserData" and may appear only when <settings> is "chrome" and <path> is "settings")

Scheme semantics:
   Used for the management of Google Chrome's setting by its user. In difference from browsers appeared before Google Chrome, its management setting appears as web-pages and not as dialog boxes.

Encoding considerations:
   Unknown, use with care.

Applications/protocols that use this scheme name:
   Works only in Google Chrome.

Interoperability considerations:
   Unknown, use with care. 
   May be unsuitable for open use on the public internet.
Security considerations:
   Unknown, use with care.
Contact:
   Registering party: Dave Thaler <dthaler&microsoft.com>
   Scheme creator: Google
Author/Change controller:
   Either the registering party or someone who is verified to represent
   the scheme creator.  See previous answer.
References:
   http://en.wikipedia.org/wiki/Google_Chrome 


(file created 2012-09-23)