(last updated 2012-09-23)

Resource Identifier (RI) Scheme name: rmi 
Status: provisional

Scheme syntax:
   rmi://<host>[:<port>]/<object-name>

Scheme semantics:
   Look up a Java object in an RMI registry.

Encoding considerations:
   Unknown, use with care.

Applications/protocols that use this scheme name:
   URI scheme understood by JNDI. Can be used to lookup a remote Java object within an RMI registry (typically for the purposes of RMI on that object). Host/port in the URI are of the rmiregistry process, not the remote object.

Interoperability considerations:
   Unknown, use with care.
   May be unsuitable for open use on the public internet.
Security considerations:
   Unknown, use with care.
Contact:
   Registering party: Dave Thaler <dthaler&microsoft.com>
   Scheme creator: Sun
Author/Change controller:
   Either the registering party or someone who is verified to represent
   the scheme creator.  See previous answer.
References:
   http://docs.oracle.com/javase/1.5.0/docs/guide/jndi/jndi-rmi.html#FORM

(file created 2012-09-23)